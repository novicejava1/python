#:kivy 1.11.1

<MyStackLayout>:
    orientation: 'lr-bt'
    Button:
        text: "sa"
        size_hint: .1, .1
        on_press: root.playSound()
    Button:
        text: "re"
        size_hint: .1, .1
        on_press: root.playSound()
    Button:
        text: "ga"
        size_hint: .1, .1
        on_press: root.playSound()
    Button:
        text: "ma"
        size_hint: .1, .1
        on_press: root.playSound()
    Button:
        text: "pa"
        size_hint: .1, .1
        on_press: root.playSound()
    Button:
        text: "da"
        size_hint: .1, .1
        on_press: root.playSound()
    Button:
        text: "ne"
        size_hint: .1, .1
        on_press: root.playSound()
    Button:
        text: "sa"
        size_hint: .1, .1
        on_press: root.playSound()
    Button:
        text: "ja"
        size_hint: .1, .1
        on_press: root.playSound()

