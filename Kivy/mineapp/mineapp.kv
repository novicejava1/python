#:kivy 1.11.1

<MyGridLayout>:
    cols: 2
    Button:
        size: .2, .2
        on_release:
            print("Button released")
            #self.text = "Button released"
            self.background_normal = "appicon.png"
    Button:
        size: .2, .2
        on_release:
            print("Button released")
            #self.text = "Button released"
            self.background_normal = "forest.jpg"
    Button:
        size: .2, .2
        on_release:
            print("Button released")
            #self.text = "Button released"
            self.background_normal = "serenity.jpg"
    Button:
        size: .2, .2
        on_release:
            print("Button released")
            #self.text = "Button released"
            self.background_normal = "smile.png"
